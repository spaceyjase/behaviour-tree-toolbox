namespace Game.Constants;

public static class Constants
{
    // The target position in global coordinates.
    public const string Target = "target";

    // The target cell in tile map coordinates.
    public const string TargetCell = "target_cell";

    // The target is a resource flag.
    public const string TargetIsResource = "target_is_resource";

    // The current resource amount key.
    public const string CurrentResourceAmount = "current_resource_amount";

    // The tile map resource key.
    public const string Resource = "Resource";

    // The tile map amount key.
    public const string Amount = "Amount";
}
